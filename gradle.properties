# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=26.2
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

# Mod Properties
# Plain semver. See CLAUDE.md: Fabric resolves dependencies with semver and
# cannot parse four-part versions, so the A.B.C.D scheme is not used here.
mod_version=0.1.0
maven_group=com.fury.terramax

# Dependencies
fabric_api_version=0.156.0+26.2
